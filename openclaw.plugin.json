{
  "id": "verificate-guard",
  "name": "Verificate Guard",
  "description": "The trust layer for OpenClaw. Gates the final answer (and, opt-in, code-writing tool calls) through Verificate's reality gates + frontier review before you trust it. Fail-open, free to try. | OpenClaw 的信任层：在信任之前，先通过 Verificate 的现实闸门 + 前沿模型评审对最终答复（以及可选的写代码工具调用）进行验证。失败即放行，免费试用。",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "token": { "type": "string", "description": "Optional Verificate API token. Omit for the free tier (25 validations/machine)." },
      "gateCode": { "type": "boolean", "default": true, "description": "Gate final answers that contain code." },
      "gateProse": { "type": "boolean", "default": false, "description": "Also gate prose answers as documents." },
      "guardToolWrites": { "type": "boolean", "default": false, "description": "Validate code inside write/patch tool calls and require approval on a reject." },
      "maxRevisions": { "type": "integer", "default": 2, "minimum": 0, "maximum": 5, "description": "Extra model passes to request when an answer is rejected." },
      "timeoutMs": { "type": "integer", "default": 20000, "description": "Per-validation network budget; on timeout the guard fails open." }
    }
  }
}
